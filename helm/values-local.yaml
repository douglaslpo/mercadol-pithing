# Local values for mercadol-pithing
# This is a YAML-formatted file.

# Global configuration
global:
  imageRegistry: ""
  imagePullSecrets: []
  storageClass: ""

# Application configuration
app:
  name: mercadol-pithing
  version: "1.0.0"
  environment: "local"
  replicas: 1

# API configuration
api:
  enabled: true
  image:
    repository: mercadol-pithing/api
    tag: "local"
    pullPolicy: IfNotPresent
  service:
    type: ClusterIP
    port: 3001
  resources:
    requests:
      memory: "128Mi"
      cpu: "100m"
    limits:
      memory: "256Mi"
      cpu: "200m"
  autoscaling:
    enabled: false

# Web configuration
web:
  enabled: true
  image:
    repository: mercadol-pithing/web
    tag: "local"
    pullPolicy: IfNotPresent
  service:
    type: ClusterIP
    port: 3000
  resources:
    requests:
      memory: "64Mi"
      cpu: "50m"
    limits:
      memory: "128Mi"
      cpu: "100m"
  autoscaling:
    enabled: false

# Embeddings service configuration
embeddings:
  enabled: true
  image:
    repository: mercadol-pithing/embeddings
    tag: "local"
    pullPolicy: IfNotPresent
  service:
    type: ClusterIP
    port: 3002
  resources:
    requests:
      memory: "256Mi"
      cpu: "250m"
    limits:
      memory: "512Mi"
      cpu: "500m"
  autoscaling:
    enabled: false

# PostgreSQL configuration
postgresql:
  enabled: true
  auth:
    postgresPassword: "postgres"
    database: "mercadol_pithing_local"
  primary:
    persistence:
      enabled: true
      size: 5Gi
    resources:
      requests:
        memory: "128Mi"
        cpu: "100m"
      limits:
        memory: "256Mi"
        cpu: "200m"

# Redis configuration
redis:
  enabled: true
  auth:
    enabled: true
    password: "redis"
  master:
    persistence:
      enabled: true
      size: 2Gi
    resources:
      requests:
        memory: "64Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"

# MinIO configuration
minio:
  enabled: true
  auth:
    rootUser: "minioadmin"
    rootPassword: "minioadmin"
  defaultBuckets: "product-images"
  persistence:
    enabled: true
    size: 10Gi
  resources:
    requests:
      memory: "64Mi"
      cpu: "50m"
    limits:
      memory: "128Mi"
      cpu: "100m"

# Prometheus configuration
prometheus:
  enabled: true
  server:
    persistentVolume:
      enabled: true
      size: 5Gi
    resources:
      requests:
        memory: "256Mi"
        cpu: "250m"
      limits:
        memory: "512Mi"
        cpu: "500m"

# Grafana configuration
grafana:
  enabled: true
  adminPassword: "admin"
  persistence:
    enabled: true
    size: 2Gi
  resources:
    requests:
      memory: "64Mi"
      cpu: "50m"
    limits:
      memory: "128Mi"
      cpu: "100m"

# Jaeger configuration
jaeger:
  enabled: true
  strategy: allInOne
  allInOne:
    resources:
      requests:
        memory: "128Mi"
        cpu: "100m"
      limits:
        memory: "256Mi"
        cpu: "200m"

# Elasticsearch configuration
elasticsearch:
  enabled: false

# Kibana configuration
kibana:
  enabled: false

# Ingress configuration
ingress:
  enabled: true
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/rate-limit: "50"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
  hosts:
    - host: mercadol-pithing-local.example.com
      paths:
        - path: /
          pathType: Prefix
          service:
            name: web
            port: 3000
        - path: /api
          pathType: Prefix
          service:
            name: api
            port: 3001
        - path: /embeddings
          pathType: Prefix
          service:
            name: embeddings
            port: 3002
  tls: []

# Network Policy configuration
networkPolicy:
  enabled: false

# Pod Disruption Budget configuration
podDisruptionBudget:
  enabled: false

# Service Monitor configuration
serviceMonitor:
  enabled: true
  labels:
    app: mercadol-pithing
  interval: 30s
  scrapeTimeout: 10s

# Cert Manager configuration
certManager:
  enabled: false

# Istio configuration
istio:
  enabled: false

# ArgoCD configuration
argocd:
  enabled: false